/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["images.unsplash.com", "cdn.sanity.io", "res.cloudinary.com", "supabase.co"],
  },
};

module.exports = nextConfig;
